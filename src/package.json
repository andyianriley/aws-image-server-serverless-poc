{
  "name": "image-resize",
  "version": "1.0.0",
  "description": "Serverless image resizing",
  "readme": "Serverless image resizing",
  "repository": "https://github.com/awslabs/serverless-image-resizing",
  "license": "Apache-2.0",
  "main": "index.js",
  "scripts": {
    "zip": "rm -fr dist && mkdir dist && zip -r dist/function.zip * && unzip -vl dist/function.zip"
  },
  "dependencies": {
    "sharp": "^0.17.3"
  },
  "devDependencies": {
    "aws-sdk": "^2.36.0"
  }
}
